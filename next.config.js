/** @type {import('next').NextConfig} */
/*const nextConfig = {
  productionBrowserSourceMaps: false,
  experimental: {
    turbo: {
      sourceMaps: false
    }
  }
};

module.exports = nextConfig;*/
